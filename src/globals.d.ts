/** The app's version, from package.json (set by vite.config.ts). */
declare const __APP_VERSION__: string;
/** When this build was made (ISO time); /version.json has the published one's. */
declare const __APP_BUILD__: string;
